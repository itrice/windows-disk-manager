{
  "name": "windows-disk-manager",
  "version": "1.0.0",
  "description": "Disk usage analysis & draft cleanup tool with a web dashboard. Scans a directory, builds analysis reports (size by folder/extension, largest files, drafts/temp/backup files, old files, empty folders) and lets you select rows in the dashboard to move to trash or permanently delete.",
  "main": "server.js",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "test": "node test/smoke.mjs"
  },
  "keywords": ["disk", "disk-usage", "cleanup", "drafts", "dashboard", "trash"],
  "license": "MIT"
}
